[providers]
python = {}

[phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[phases.install]
onlyIncludeFiles = ["requirements.txt", "src/"]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn src.app:app --host 0.0.0.0 --port $PORT"

[variables]
NIXPACKS_NO_CACHE = "1"